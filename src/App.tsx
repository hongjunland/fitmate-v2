import { ChangeEvent, useState } from "react";
import {
  RecoilRoot,
  atom,
  selector,
  useRecoilState,
  useRecoilValue,
  useSetRecoilState,
} from "recoil";
import UserInfo from "./UserInfo";
function App() {
  return (
    <RecoilRoot>
    </RecoilRoot>
  );
}

export default App;
